# TechLab Fisio — configurações do gerenciador de pacotes.
# No pnpm 12+, configurações de projeto (engineStrict, saveExact, allowBuilds)
# são definidas exclusivamente em `pnpm-workspace.yaml`.
# Este arquivo é reservado para configurações de registry e autenticação do npm/pnpm, se necessárias.
